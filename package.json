{
  "name": "photobot-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Photography community Discord bot with admin dashboard",
  "license": "MIT",
  "author": "Kevin Lee",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kevinkiklee/photobot.git"
  },
  "homepage": "https://github.com/kevinkiklee/photobot#readme",
  "bugs": {
    "url": "https://github.com/kevinkiklee/photobot/issues"
  },
  "keywords": [
    "discord",
    "bot",
    "photography",
    "nextjs",
    "prisma"
  ],
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "init:local": "./scripts/setup.sh",
    "dev": "pnpm -r dev",
    "dev:local": "./scripts/dev.sh 2>&1 | tee /tmp/photobot-dev.log",
    "dev:down": "docker compose down",
    "build": "pnpm --filter @photobot/db build && pnpm --filter @photobot/bot build && pnpm --filter @photobot/dashboard build && pnpm --filter @photobot/voting build",
    "test": "vitest run",
    "test:integration": "DATABASE_URL=postgresql://postgres:postgres@localhost:54422/postgres vitest run --workspace vitest.integration.workspace.ts",
    "test:all": "pnpm test && pnpm test:integration",
    "status": "./scripts/status.sh",
    "db": "./scripts/db.sh",
    "cleanup": "./scripts/cleanup.sh",
    "seed:prompts": "tsx scripts/seed-prompts.ts"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "sharp",
      "esbuild"
    ]
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.3",
    "vitest": "^4.1.2"
  }
}
